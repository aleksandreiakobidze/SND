import type { LatLngTuple } from "leaflet";

/** Approximate geographic center of Georgia — default map view before fitBounds / markers. */
export const MAP_DEFAULT_CENTER_GEORGIA: LatLngTuple = [42.315, 43.3566];

/** Zoom level that frames Georgia (country) when no bounds are applied yet. */
export const MAP_DEFAULT_ZOOM_GEORGIA = 8;
